/**
 * Whitespaces
 *
 * @export
 * @type {RegExp}
 *
 * @description _Matches all single and repeating whitespace characters in a string._
 * @since v0.0.1
 */

export const whitespaces: RegExp = /\s+/g;
