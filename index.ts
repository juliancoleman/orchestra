/**
 * Orchestra
 *
 * First, import everything into the `orchestra` namespace
 * and export as default so that the user can do the
 * following:
 *
 *   import orchestra from "@juliancoleman/orchestra"
 *
 * Then export the remaining members so that the user can
 * also do:
 *
 *   import { capitalize } from "@juliancoleman/orchestra";
 */

import * as orchestra from "./src/index";

export default orchestra;
export * from "./src/index";
